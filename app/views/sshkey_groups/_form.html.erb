<%= form_for(@sshkey_group) do |f| %>
  <% if @sshkey_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sshkey_group.errors.count, "error") %> prohibited this sshkey_group from being saved:</h2>

      <ul>
      <% @sshkey_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
